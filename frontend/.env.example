# En dev NO hace falta definir nada: el proxy de Vite reenvía /api y /auth al
# backend en http://127.0.0.1:8080.
#
# En producción (frontend en Vercel, backend en otra URL), definí:
# VITE_API_BASE=https://api.tu-dominio.com
VITE_API_BASE=
